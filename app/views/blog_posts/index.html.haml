.small-12
  .row
    .row
      .large-12.columns
        #slider.small-centered.small-10.columns
          %ul{ "data-orbit" => ""}
            - @blog_posts.first(3).each do |blog|
              %li
                = image_tag blog.blog_cover(:large), style: "width:1000px;height:400px;"
                .orbit-caption.text-centered
                  %h3
                    = link_to blog.title, blog
    / / Call to Action Panel
    / .row
    /   .large-12.columns
    /     .panel
    /       %h4 Get in touch!
    /       .row
    /         .large-9.columns
    /           %p Would you like to be featured in our blog?  Contact us and let us know.  Let us promote your new album to the masses.
    /         .large-3.columns
    /           %a.radius.button.right{:href => "#"} Contact Us
    .row
      .text-centered
        %h4.title-bar
          Featured Albums
      .blog-entries
        - @blog_posts.each_with_index do |blog, index|
          .blog-post.large-6.columns
            .panel-header
              %h4= link_to truncate(blog.title, length: 120), blog
              %p
                Posted
                = blog.created_at.strftime("%B %d, %Y")
            .blog-content.panel
              .blog-image
                = image_tag blog.blog_cover.url(:medium), style: "width:100%;height:100%"
              .blog-player
                - if blog.soundcloud_url
                  = blog.soundcloud_url.html_safe
                - else
                  .empty-player
              .blog-text.panel
                = sanitize truncate(blog.post.to_html, length:350)
                - if blog.post.to_html.length > 350
                  = link_to 'read more...', blog
          - if (index + 1).even?
            .clear-row.left.full-width

    / Footer
    %footer.row
      .large-12.columns
        %hr/
        .row
          .large-6.columns
            %p © Copyright Audioprint 2013.
          / .large-6.columns
          /   %ul.inline-list.right
          /     %li
          /       %a{:href => "#"} Link 1
          /     %li
          /       %a{:href => "#"} Link 2
          /     %li
          /       %a{:href => "#"} Link 3
          /     %li
          /       %a{:href => "#"} Link 4