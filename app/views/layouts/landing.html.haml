!!!
%html
  %head
    =javascript_tag "var AUTH_TOKEN = #{form_authenticity_token.inspect};" if protect_against_forgery?
    %link{href: 'http://fonts.googleapis.com/css?family=Lato:300,400,700,900,300italic,400italic,700italic,900italic', rel:'stylesheet', type:'text/css'}
    %meta{:content => "width=device-width, initial-scale=1.0", :name => "viewport"}
    %title= content_for?(:title) ? yield(:title) : "Audioprint()"
    %meta{:content => content_for?(:description) ? yield(:description) : "Audioprint()", :name => "description"}
      = stylesheet_link_tag "application", :media => "all"
      = javascript_include_tag "application", "https://js.stripe.com/v2/"
      = csrf_meta_tags
      = yield(:head)
  %body.login-container{:class => "#{controller_name} #{action_name}"}
    / = render 'layouts/navigation'
    / = render 'layouts/messages'
    = yield
    / %header.navbar.navbar-fixed-top.navbar-inverse{style: "position:fixed;"}
    /   %nav.navbar-inner
    /     .container{style: "margin-left: 20px;"}
    /       = render 'layouts/navigation'
    .welcome
      .visible-phone
        %h1 Welcome to the future of Music Distribution
        %h2 Sign up and see how you can reach new customers!
        = link_to new_user_registration_path do
          .btn.btn-inverse.btn-large
            Create An Account Today!
      .hidden-phone
        %h1 Welcome to the future
        %h1 of Music Distribution
        %h2 Sign up and see how you can reach new customers!
        = link_to new_user_registration_path do
          .btn.btn-inverse.btn-large
            Create An Account Today!

